{
  "index_patterns": [
    "suricata-*",
    "zeek-*",
    "syslog-*",
    "red-team-events-*",
    "ir-events-*"
  ],
  "priority": 100,
  "_meta": {
    "description": "Adversary-in-a-Box lab telemetry indices (P7/S3). Attaches the aib-retention-7d ILM policy and single-node-friendly settings to every lab index pattern. Composable template; applies only to indices created after it is installed."
  },
  "template": {
    "settings": {
      "index.lifecycle.name": "aib-retention-7d",
      "index.number_of_replicas": 0
    }
  }
}
