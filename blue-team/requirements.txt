flask==3.1.3
requests==2.34.2
# Phase A1: bumped from 6.0.1 — pysigma-backend-elasticsearch 1.1.6 requires
# pyyaml>=6.0.2, conflict broke CI dependency install (run 26348906189).
pyyaml==6.0.3
elasticsearch==8.13.0
rich==15.0.0
click==8.4.1
python-dateutil==2.9.0
# OQ-2 (ADR 0001): Sigma → EQL compile pipeline.
# Audit-2 Gap #8: pySigma-backend-elasticsearch 1.1.7 was never published to
# PyPI (verified via `pip index versions`) — CI installs failed. 1.1.6 is
# the closest published version in the same minor line.
sigma-cli==1.0.4
pySigma-backend-elasticsearch==1.1.6
