flask==3.1.3
requests==2.34.2
# Phase A1: bumped from 6.0.1 — pysigma-backend-elasticsearch 1.1.6 requires
# pyyaml>=6.0.2, conflict broke CI dependency install (run 26348906189).
pyyaml==6.0.3
elasticsearch==9.4.1
rich==15.0.0
click==8.4.1
python-dateutil==2.9.0.post0
# OQ-2 (ADR 0001): Sigma → EQL compile pipeline.
# sigma-cli 3.0.2 requires pysigma>=1.3.0; pySigma-backend-elasticsearch
# 1.1.6 caps pysigma<0.12.0, so the backend has to bump to its 2.x line
# in lock-step. Verified resolution with `pip install --dry-run`.
sigma-cli==3.0.3
pySigma-backend-elasticsearch==2.0.3
